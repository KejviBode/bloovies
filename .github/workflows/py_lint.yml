on:
  workflow_call:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  tests:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v33
      - run: |
          pip install -r requirements.txt
          pytest
  linting:
    name: linting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v33
      - run: |
          pip install -r requirements.txt
          pylint --fail-under=8 *.py
